metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: oci-cp

spec:
  matchLabels:
    # Applied manually in Omni UI after OCI nodes connect via SideroLink.
    # omnictl machine set-labels <machine-id> role=oci-cp
    role: oci-cp
